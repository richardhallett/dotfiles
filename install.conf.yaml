- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bash:
    ~/.bashrc:
    ~/.gitconfig:

- create:
    - ~/.vim/undo-history
    - ~/projects

- shell:
  - [git submodule update --init --recursive, Installing submodules]